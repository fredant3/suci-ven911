# from django.urls import include, path

urlpatterns = [
    # path("",DotacionListApiView.as_view(),name="list"),
    # path("create",DotacionCreateApiView.as_view(),name="create"),
    # path("<int:pk>/update",DotacionUpdateApiView.as_view(),name="update"),
    # path("<int:pk>/delete",DotacionDeleteApiView.as_view(),name="delete"),
    # path("export/excel",DotacionExcelView.as_view(), name="export_excel"),
]
